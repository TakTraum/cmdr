<UserControl x:Class="cmdr.Editor.Views.CommentEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             xmlns:ddButton="clr-namespace:cmdr.WpfControls.DropDownButton;assembly=cmdr.WpfControls"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <ContentControl MinHeight="30" MinWidth="50" Style="{StaticResource ResizeStyle}">
            <TextBox TextWrapping="Wrap" Text="{Binding Comment, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Left" Width="219" />
            <ddButton:DropDownButton Content="Select" Width="50" HorizontalAlignment="Right" 
                                             MenuPlacement="Left"
                                             Command="{Binding SelectCommentsCommand, Mode=OneTime}" 
                                             ItemsSource="{Binding CommentsMenu, Mode=OneTime}" Margin="229,15,21,251" />
        </ContentControl>
    </Grid>
</UserControl>
